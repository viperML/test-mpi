spack:
  view: true
  concretizer:
    unify: true

  specs:
  - openmpi @4
  - py-mpi4py
  - python

  mirrors:
    gh-buildcache:
      url: oci://ghcr.io/viperml/test-mpi
      signed: false

  packages:
    all:
      require:
      - target=x86_64
